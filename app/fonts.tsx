import { Montserrat, Tinos, Rouge_Script, Tangerine } from "next/font/google";

export const heading = Tinos({ subsets: ["latin"], weight: "400" });
export const italics = Tangerine({ subsets: ["latin"], weight: "400" });
export const text = Montserrat({ subsets: ["latin"] });